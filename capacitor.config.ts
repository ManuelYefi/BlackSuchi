import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.blacksushi.app',
  appName: 'BlackSushi',
  webDir: 'dist/blacksushi/browser'
};

export default config;